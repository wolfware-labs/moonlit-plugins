[package]
name = "moonlit-plugin-gitlab"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
license = "MIT OR Apache-2.0"
publish = false
description = "Moonlit first-party gitlab plugin."

[lib]
name = "gitlab"
crate-type = ["cdylib"]

[dependencies]
moonlit-sdk = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
tempfile = "3"
